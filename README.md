# Kinderwell

A parenting-education iOS app (React Native / Expo), live on the App Store. Short daily lessons behind a hard subscription paywall.

## Start here

- **`CLAUDE.md`** (repo root) — the entry point for any AI coding session: architecture, invariants, environment-safety rules, conventions. Read it first.
- **`docs/README.md`** — the docs index (evergreen process docs + living trackers).

## Dev

- Node 20 (`.nvmrc`).
- Typecheck: `npx tsc --noEmit` · Lint: `npx eslint .` · Tests: `npm test`

That's it — this README's only job is to route you to the real docs.
