# Content errata — Sprinklers transcription

Issues found during the faithful transcription of the hand-built Sprinklers
screens into `src/lessons/content/sprinklers.ts`. Text was copied VERBATIM
(errors preserved); each observed problem is logged here rather than silently
fixed.

- sprinklers §5 screen1–6: `totalSteps={7}` is declared in every Section 5 screen, but the section only has 6 screens (SprinklersSec5Screen1..6, with `currentStep` running 1→6) — the progress denominator overshoots the real screen count by one.
- recordingDeepBondMoments §1 screen1–6: all quotation marks and apostrophes are straight ASCII (`"..."`, `weren't`, `don't`, `it's`, `you'll`, `can't`) rather than the curly typographic quotes (`"…"`, `'`) used across the other converted lessons — inconsistent typography. Transcribed verbatim as-is.

# Content errata — Emotional Sandbags transcription

Issues found during the faithful transcription of the hand-built Emotional
Sandbags screens into `src/lessons/content/emotionalSandbags.ts`. Text was
copied VERBATIM (errors preserved); each observed problem is logged here rather
than silently fixed.

- emotionalSandbags §3–§6 (all screens): quotation marks and apostrophes are straight ASCII (`"..."`, `isn't`, `it's`, `you're`, `wasn't`, `can't`, `Let's`, `you've`, `didn't`) rather than the curly typographic quotes (`"…"`, `'`) used in §1–§2 of the same lesson and in the other converted lessons — inconsistent typography within the lesson. Transcribed verbatim as-is.
- emotionalSandbags §5 screen6: the emotion-category labels embed literal emoji glyphs in the header text (`❌ DON'T USE (Too Broad)`, `🔵 UPSET EMOTIONS`, `🔴 ANGRY EMOTIONS`, `🟢 STRESSED / LOW-ENERGY`, `🟣 CONFUSION / SOCIAL PAIN`, `🟡 HAPPY EMOTIONS (for contrast later)`) — kept verbatim in the callout labels.
- emotionalSandbags §2 screen3: the original screen defines unused label-position styles (`label1/label2/label3`) and ends with a stray comment `// Re-adjusting labels in the code snippet for clarity`; the three floating labels (`STRESS`, `ANGER`, `OVERWHELM`) are absolutely-positioned decorations layered over the 🎒 emoji circle and carry no body text — the hero emoji is transcribed but the decorative overlay labels are not reproducible as data blocks.
- emotionalSandbags §1 screen3 / §2 screen4 / §5 screen8 & others: several screens open with a decorative Ionicon inside a coloured circle (e.g. `bulb-outline`, `people-outline`) rather than an emoji. The schema's `heroEmoji` block carries an emoji, not an Ionicon, so these purely-decorative Ionicon hero circles are omitted (no text is lost).
- emotionalSandbags §4 screen2–5: these are interactive accordions (one of four steps expanded, the rest collapsed as tappable previews). Transcribed as a static step-list `cardList` plus the expanded step's content blocks; the collapse/expand interaction is not reproducible in the data model.

# Content errata — Helping Someone Process Emotions transcription

Issues found during the faithful transcription of the hand-built Helping Someone
Process Emotions screens into `src/lessons/content/helpingProcessEmotions.ts`.
Text was copied VERBATIM (errors preserved); each observed problem is logged here
rather than silently fixed.

- helpingProcessEmotions §1–§2 (all screens): quotation marks and apostrophes are straight ASCII (`aren't`, `They're`, `you'll`, `Let's`, `That's`, `She's`, `don't`, `it's`, `Everyone's`, `I'm`, `you're`, `there's`, `didn't`, `wasn't`, `you can't`, `"fix it"`, `"perfect"`) rather than the curly typographic quotes (`"…"`, `'`) used across the other converted lessons — inconsistent typography. Transcribed verbatim as-is.
- dissociation §1–§4 (all screens): quotation marks and apostrophes are straight ASCII (`"..."`, `don't`, `can't`, `I've`, `isn't`, `there's`, `Let's`, `you're`, `child's`, `doesn't`, `you'll`, `I'm`, `I'll`, `That's`, `it's`, `dissociation's`) rather than the curly typographic quotes (`"…"`, `'`) used across the other converted lessons — inconsistent typography. Transcribed verbatim as-is.
- dissociation §1 screen4: the original screen is a multi-part comparison layout — a scenario card ("Imagine this:" + setup + a quote box), a divider, two coloured option cards (Option A — No dissociation, green `#E8F5E9`; Option B — Dissociation, orange `#FFF4E6`), then a Key-insight box. There is no comparison/two-column or nested quote-in-card block in the schema, so each region is transcribed as a separate `callout` (scenario summary, the quote as its own summary callout with the quote repeated as label+line, the two option cards as coloured summary callouts with the option name as `label`, and the key insight). The nested visual hierarchy and column styling are not reproduced; all text is preserved verbatim.
- dissociation §4 screen1: the three "Today we learned" bullets embed a hard line break (`\n`) inside the first bullet's text; kept verbatim inside the single `cardList` item title (the schema has no per-item multi-line/subtitle rendering for bullets).
- helpingProcessEmotions §2 screen1: the original screen is a Dad/Daughter dialogue card (`dialogueCard` with bold `speaker:` labels above italic `dialogue` lines) — there is no dialogue block in the schema, and the `cardList` block renders only an item title (subtitle is dropped). To preserve ALL dialogue text verbatim, each exchange is transcribed as a `paragraph` with the speaker name as an inline `emphasis` span followed by the plain dialogue text; the closing `helperBox` ("Notice what Dad isn't doing.") is transcribed as the italic `footer`. The two-column card styling is not reproduced.

# Content errata — Naming our Emotions transcription

Issues found during the faithful transcription of the 24 hand-built Naming
Emotions screens (Sub1..4, Screen1..6) in `src/screens/naming-emotions/` into
`src/lessons/content/namingEmotions.ts`. Text was copied VERBATIM (errors
preserved); each observed problem is logged here rather than silently fixed.

- namingEmotions §1–§4 (all screens): quotation marks and apostrophes are straight ASCII (`"..."`, `Let's`, `doesn't`, `We're`, `It's`, `don't`, `you're`, `there's`, `That's`, `you'll`, `You've`) rather than the curly typographic quotes (`"…"`, `'`) used in some other lessons. The screen-3 headings use a real Unicode horizontal ellipsis glyph `…` (U+2026) in "At the surface, this felt like…", and the callouts use a real em dash `—` (U+2014) and en dash `–` (U+2013, "2–3 emotions"). All transcribed verbatim as-is.
- namingEmotions §1–§4 screen2 / screen4 / screen5 (RESOLVED — engine extended): these are genuinely interactive screens — a free-text multiline `TextInput` ("What happened?", placeholder "Describe the moment briefly. Where were you? Who was involved?"), and emotion-picker screens (a picker button opening the shared `EmotionPicker` modal, with a conditional "Why did you feel {emotion}?" follow-up `TextInput`). Rather than drop the interactivity, two block types were added to the SPEC-09 engine (Phase 3): `textInput` and `emotionPicker` (see `schema.ts` / `BlockRenderer.tsx`). Screen 2 → `textInput`; screens 4 & 5 → `emotionPicker` (reusing the existing global `EmotionPicker` component verbatim). The user's input is EPHEMERAL — held in local state and discarded on completion — exactly as in the hand-built screens (which never persisted it). The `LessonController` gates its Next button (visible-but-disabled) until each required input is satisfied, mirroring the source's `disabled={value.trim().length === 0}` / `disabled={!canContinue}`.
- namingEmotions §1–§4 screen3 (RESOLVED — chip colours supported): the emotion "chip" (`HAPPY`/`SAD`/`MAD`/`BAD`) is a single centred pill with one-off per-sublesson colours — HAPPY: bg `#FFFBEB`, border `#F59E0B`, text `#D97706`; SAD: bg `#EFF6FF`, border `#3B82F6`, text `#2563EB`; MAD: bg `#FEF2F2`, border `#EF4444`, text `#DC2626`; BAD: bg `#F3F4F6`, border `#6B7280`, text `#4B5563`. The `cardList` chip item schema was extended with optional `color` (bg), `borderColor`, and `textColor` fields, so these distinct topic colours ARE now reproduced (single-item `chips` `cardList`).
- namingEmotions §1–§4 screen3 + screen6: the source "messageBox" and "infoBox" are `Colors.primaryBg`-tinted, left-aligned rounded boxes holding multiple body lines. They are transcribed as `callout` `highlight` blocks (each source line a separate callout line). The default `highlight` renderer centres text and uses `Colors.surface`, so the tint colour and left alignment are not byte-identical; text is preserved verbatim.
- namingEmotions §1–§4 screen6: the original completion screens are content screens (headline + tinted info box), NOT the sparse canonical `sectionComplete` card. Per the SPEC-09 controller model (all-`content`, like dissociation.ts), they are transcribed as `content` screens; the controller writes the section id ('1'..'4') to the storage key on each section's last screen, reproducing markSubLessonComplete's `['1','2','3','4']` array.
- namingEmotions §1–§4 screen2/4/5 (behavioural, minor): the hand-built input screens wrap their body in a `KeyboardAvoidingView` (content pinned top via `justifyContent: 'space-between'`). The generic `LessonController` renders content centred inside the shared `LessonContainer`, which is itself a `ScrollView` — so the keyboard cannot clip the field (it scrolls into view), but the exact push-up animation and top-pinned layout differ slightly. Not reproduced at the block level to avoid adding `KeyboardAvoidingView` to the controller for all ~300 screens; the ScrollView is a functional equivalent.
- namingEmotions §4 screen5: the picker button label here is "Select an emotion" (not the "(optional)" variant), and the code comment says "Require 2 emotions minimum (not optional)" while the disable logic (`selectedEmotion && why.trim().length > 0`) still allows advancing after a single second emotion — a minor logic/label inconsistency in the source, noted but not altered. Transcribed with the source's actual heading ("What was another emotion present?") and helper ("Try to identify at least one more.").

# Content errata — Serve and Return

Issues found during the faithful transcription of the 22 hand-built Serve &
Return screens (Sec1..6) in `src/screens/serveReturn/` into
`src/lessons/content/serveReturn.ts`. Text was copied VERBATIM (errors
preserved); each observed problem is logged here rather than silently fixed.

- serveReturn §1–§6 (all screens): quotation marks and apostrophes are straight ASCII (`"..."`, `you'll`, `it's`, `isn't`, `Doesn't`, `don't`, `I'm`, `can't`, `Let's`, `week's`, `child's`, `you're`, `someone's`, `We'll`, `There's`, `You're`) rather than the curly typographic quotes (`"…"`, `'`) used in some other converted lessons — inconsistent typography. Transcribed verbatim as-is. Note the em dash `—` (U+2014) in §1 screen1 ("70% of serves" line uses a real `—`) and the no-space em dash joins (`childhood—it's`, `communication.`, `connection—one`) are all straight-transcribed. §5 screen3 "Missing some serves is normal — and human." uses a spaced em dash.
- serveReturn §1 screen4 / §2 screen4 / §3 screen4: three `QuizQuestion` screens with a single question each (`totalQuestions={1}`). §1 screen4 writes `markSectionComplete('1')` and §2 screen4 writes `'2'` on correct answer (last screen of their sections); §3 screen4 instead NAVIGATES to screen5 on correct (screen5 is §3's real last screen and writes `'3'`). Transcribed as `quiz` blocks; the controller writes the section id on each section's last screen (screens 4/4/5 respectively), reproducing the `['1','2','3','4','5','6']` array.
- serveReturn §3 screen5 (OPTIONAL text input): the reflection field's `TextInput` (placeholder "Your reflections (optional)") is NOT gated — the source's "Finish" button (`onPress={handleFinish}`) has no `disabled` logic, so the field is genuinely optional. Modelled with a `textInput` block, `required: false`, `minHeight: 120` (source uses `minHeight: 120`). The source has NO bold headline above the input (the screen's `header` "Think About It" is the heading and a body paragraph precedes the input), so the `textInput` block's required `headline` field is set to the empty string `''`; the block therefore renders no headline text, matching the source. The user's input is EPHEMERAL (local `useState`, discarded on completion), matching the hand-built screen.
- serveReturn §1 screen2: the "back-and-forth" steps are a 2-item numbered card (`stepsCard`: circular primary-coloured number badges + text). Transcribed as a `surface` `cardList` with `number: 1` / `number: 2`. The renderer's number circle is a neutral surface-coloured badge rather than the source's filled `Colors.primary` circle with white text — the numbering is preserved but the badge colour differs.
- serveReturn §1 screen3: the tennis-match metaphor rows are emoji-led cards (🎾, 🏓, 🔄) in a single white `metaphorCard`. Transcribed as a `surface` `cardList` with emoji icons. The source groups all three rows inside one shared card; the `cardList` renders three separate surface cards — visually similar but not one enclosing box.
- serveReturn §2/§3/§4/§5/§6 (titled bullet/quote boxes): most content is coloured, titled rounded boxes holding bulleted or quoted lines — green `#E8F5E9`, amber `#FFF9E6`, `Colors.primaryBg`, blue `#E3F2FF`, and white surface. There is no titled-list block in the schema, so each is transcribed as a `callout` (`summary` for titled bullet/example boxes with the card title as `label`; `insight`/`quote`/`preview`/plain `summary` for the accent boxes). The source's exact background colour and LEFT text alignment are approximated: `summary` callouts render left-accented but the bullet `•` / check `✓` glyphs and coloured bullet colours are NOT reproduced (the lines render as plain centred/left callout lines without bullet markers). One-off box backgrounds are carried via `bg` where a green/amber/blue box is distinctive; boxes on `Colors.primaryBg` use the renderer default. All line text is preserved verbatim.
- serveReturn §4 screen1–3 / §5 screen1–3 / §6 screen1: the hero graphic is a large emoji (👋, 👁️, 📱, 🤗, 🧠, 👨‍👩‍👧, 👀) centred in a tinted rounded SQUARE box (`imageContainer`, `borderRadius` lg, `padding: 40`, emoji `fontSize: 80`). Modelled with the `heroEmoji` block, which renders the emoji in a tinted CIRCLE (100×100, `fontSize: 50`) — the shape (circle vs rounded square) and emoji size differ, but the emoji and its tint colour (`bg`: `#F5F9FF` for primaryBg boxes, `#E8F5E9` green, `#FFF9E6` amber, `#E3F2FF` blue) are preserved. (`Colors.primaryBg` is approximated as `#F5F9FF` to match the callout `summary` default tint; its exact theme value was not resolved.)
- serveReturn §4 screen1 / §5 screen1 / §6 screen1: only the FIRST screen of §4, §5, §6 carries a "Section N of 6" eyebrow label (`sectionLabel`); the other screens in those sections, and ALL of §1–§3, have no such label. Transcribed via the screen-level `label` field ('Section 4 of 6' / 'Section 5 of 6' / 'Section 6 of 6') on those three screens only, faithfully reproducing the source's inconsistent labelling. §1–§3 use no `label` at all (matching source, which shows only the `LessonContainer` step progress).
- serveReturn §5 screen3: the stats box contains an inline-emphasised span — "returning about **70% of serves** is enough…" (`statsHighlight`: bold, `Colors.primary`). Transcribed as a `paragraph`-style rich-text array inside a `summary` callout line, with "70% of serves" carrying `emphasis: 'emphasis'`. The callout renderer's emphasis style (primary + bold) matches the source's highlight styling.
- serveReturn §6 screen3: the "This week's practice" checklist uses literal `✓` check glyphs (`checkmark` text, green `#2E7D32`) as list markers. Transcribed as a `plain` `cardList` with `✓` as an emoji icon on each item; the check glyph is preserved but its green colour is not (emoji icons render as-is). The closing box (`transitionBox`, "We'll build on this in the next lesson.") is a muted surface preview — transcribed as a `preview` callout.
- serveReturn §1–§6 (all sections): none of the section-final screens is the sparse canonical `sectionComplete` card. §1/§2/§3 end on a quiz or reflection screen; §4/§5/§6 end on a normal content screen. Per the SPEC-09 controller model (all-`content`, like dissociation.ts / namingEmotions.ts), every screen is `content`; the controller writes the section id on each section's last screen, reproducing `markSectionComplete`'s `['1'..'6']` array.
- serveReturn heading sizes: source headers are 28px on §1 screen1 (mapped to `size: 'xl'`) and 26px everywhere else (mapped to `size: 'lg'`). The engine's `xl` = 28px and `lg` = 22px, so the 26px headers render slightly smaller than the source; chosen `lg` as the closest available size (matching the reference lessons' convention).

# Content errata — Labeling Emotions

Issues found during the faithful transcription of the 23 hand-built Labeling
Emotions section screens (Lesson5Sec{1..4}Screen*) in `src/screens/lessons/`
into `src/lessons/content/labelingEmotions.ts` (Sec1: 7 screens, Sec2: 4,
Sec3: 8, Sec4: 4). Text was copied VERBATIM (errors preserved); each observed
problem is logged here rather than silently fixed. `Lesson5Complete.tsx` is a
hub-level "whole lesson done" screen outside any section flow (writes no
progress) and is intentionally NOT transcribed.

- labelingEmotions §1–§4 (all screens): quotation marks and apostrophes are straight ASCII (`"..."`, `'...'`, `child's`, `don't`, `It's`, `I'm`, `wouldn't`, `can't`, `Let's`, `Here's`, `driver's`, `isn't`, `they're`, `couldn't`, `you'll`) rather than the curly typographic quotes (`"…"`, `'`) used in some other converted lessons — inconsistent typography. Note the real em dash `—` (U+2014) appears in several places ("Highway 1", "less tension", "— Matthew Lieberman, UCLA", "— Dr. Sarah Watamura", "our own emotions — and when"). All transcribed verbatim as-is.
- labelingEmotions §1 (progress denominator): every Section 1 screen declares `totalSteps={8}` in its `LessonContainer`, but the section has only 7 screens (Sec1Screen1..7, with `currentStep` running 1→7). The `LessonContainer` step denominator overshoots the real screen count by one. `markSectionComplete('1')` is called on Screen7 (the real last screen). Modelled as a 7-screen section.
- labelingEmotions §3 (progress denominator): every Section 3 screen declares `totalSteps={9}`, but the section has only 8 screens (Sec3Screen1..8, `currentStep` 1→8). The step denominator overshoots by one. `markSectionComplete('3')` is called on Screen8 (the real last screen). Modelled as an 8-screen section.
- labelingEmotions §1/§2/§3 (quiz screens): three screens use the `QuizQuestion` component (Sec1Screen7, Sec2Screen3, Sec3Screen8), all single-question (`totalQuestions={1}`). Transcribed as `quiz` blocks. Sec1Screen7 writes `markSectionComplete('1')` on correct answer (it is §1's last screen); Sec3Screen8 writes `'3'` (§3's last screen); Sec2Screen3 instead NAVIGATES to Screen4 on correct (Screen4 is §2's real last screen and writes `'2'`). The controller writes the section id on each section's last screen, reproducing the `['1','2','3','4']` array.
- labelingEmotions §1–§4 (section-final screens): none of the section-final screens is the sparse canonical `sectionComplete` card. §1 and §3 end on a quiz screen; §2 and §4 end on a normal content screen (quote / conclusion boxes). Per the SPEC-09 controller model (all-`content`, like dissociation.ts / serveReturn.ts), every screen is `content`; the controller writes the section id on each section's last screen.
- labelingEmotions heading sizes: source headlines are uniformly 28px (mapped to `size: 'xl'`). Several screens also have a secondary bold emphasis line ("Why?", "Oddly, I felt a lot better.", "But in reality, I felt a lot better.", "Of course not.", section-intro sub-statements) rendered as bold body text (fontSize 19–20, `Colors.textPrimary`/`Colors.primary`) — there is no bold-body-line block in the schema, so these are transcribed as `heading` `size: 'lg'` (the engine's `lg` = 22px, close but not byte-identical; some source lines were 19–20px). Text preserved verbatim.
- labelingEmotions §1 screen1: the two "Why?" points are a bulleted list (`•` glyph via `bullet` style). Transcribed as a `plain` `cardList` with `•` as an emoji icon on each item; the bullet glyph is preserved but its exact left-aligned inline layout differs slightly from the renderer's icon-slot layout. §3 screen5's "But underneath, he might feel:" list uses no explicit bullet glyph (plain lines) — transcribed as a titled `summary` callout with each item a separate line.
- labelingEmotions (coloured/titled boxes → callouts): most content sits in coloured, sometimes titled rounded boxes — `Colors.primaryBg` (approximated `#F5F9FF`), green `#E8F5E9`, amber/`#FFF9E6`, `#FFF3E0` (orange-bordered), `#F5F5F5`, `#E3F2FF` (blue), `#FFE4ED` (pink), and white surface. There is no titled-list block in the schema, so each is transcribed as a `callout` `summary` with the box's label as `label` (e.g. `UCLA BRAIN MAPPING CENTER`, `TOO BROAD / DON'T USE`, `FRUSTRATION`, `WORRY`, `But underneath, he might feel:`) and its lines as callout lines. One-off box backgrounds are carried via `bg`; accent/border colours via `accentColor`; distinctive green/orange text via `textColor`; label colours via `labelColor`. The source's exact left-vs-centre alignment, border-left accents, and 2px full borders are approximated by the renderer; all text preserved verbatim.
- labelingEmotions §3 screen4 ("Deeper Emotions"): the source is a single white `emotionsBox` containing two labelled rows (`FRUSTRATION` + description, a divider, `WORRY` + description). The schema has no label+description card row, so the two rows are transcribed as two consecutive `summary` callouts (label = emotion word, line = description); the FRUSTRATION callout carries `dividers: true` as the closest analogue to the source's inter-row divider, though with a single line it renders no visible divider. The enclosing single-box grouping is not reproduced.
- labelingEmotions §2 screen2 / §3 screen3 & screen5: several `heroEmoji` sit at larger source sizes (🚁 fontSize 64; others 48/56). The `heroEmoji` renderer uses a fixed 50px glyph in a 100×100 tinted circle, and the source emojis here sit on NO circle (bare centred emoji). The emoji is preserved; the decorative circle/size differ.
- labelingEmotions §2 screen1 / §3 screen1 / §4 screen1: the "SECTION N OF 4" eyebrow appears only on the FIRST screen of each section (and §1 screen1). Transcribed via the screen-level `label` field ('SECTION 1 OF 4' … 'SECTION 4 OF 4') on those screens only, matching the source. Other screens carry no label.
- labelingEmotions (CTA button titles): the source Next/Continue buttons include a literal trailing arrow glyph in their title (`Next →`, `Continue →`). Transcribed verbatim into each screen's `cta`.

# Content errata — Communication Mistakes

Issues found during the faithful transcription of the 59 hand-built Communication
Mistakes screens into `src/lessons/content/communicationMistakes.ts`. The source
is SPLIT across two directories (same lesson, same component name): Sec1..Sec3 in
`src/screens/communication-mistakes/` (DASH dir), Sec4..Sec13 in
`src/screens/communicationMistakes/` (CAMEL dir). Per-section screen counts:
[6, 9, 6, 4, 7, 3, 2, 3, 1, 3, 4, 6, 5] = 59. Text was copied VERBATIM (errors
preserved); each observed problem is logged here rather than silently fixed.

- communicationMistakes §1–§13 (all screens): quotation marks and apostrophes are straight ASCII (`"..."`, `'...'`, `you'll`, `didn't`, `she's`, `it's`, `isn't`, `wasn't`, `don't`, `weren't`, `that's`, `there's`, `I'm`, `I'll`, `Let's`, `you're`, `you've`, `we'll`, `Paige's`, `doesn't`, `shouldn't`, `couldn't`, `someone's`, `others'`) rather than the curly typographic quotes (`"…"`, `'`). Note the real en dash `–` (U+2013) in §1 screen1 ("parent–child") and real em dashes `—` (U+2014) throughout (used both spaced "help —" and as no-space joins like "invalidating—and", "understood—hoping", "instinct—especially", "nothing—but", "moment—or"). Also literal emoji glyphs embedded in body text (👉 in §5 screen1, 🎉 in §13 screen5) and the `≠` glyph in §5 screen5 ("Acknowledging ≠ Agreeing"). All transcribed verbatim as-is.
- communicationMistakes (section titles): section titles use the hub sublesson wording from `CommunicationMistakesLessonScreen.tsx` (`subLessons[].title`, ids '1'..'13'), not each screen's on-screen header. This matters for §9, whose hub title is "Common Minimizing Phrases" while its single screen's header is "When help misses the point"; the hub wording is used for `section.title` (consistent with serveReturn.ts / labelingEmotions.ts which take section titles from the lesson hub).
- communicationMistakes (section-id mapping): each section's LAST screen calls `markSectionComplete('N')` (verified via grep: §1→Screen6, §2→Screen9, §3→Screen6, §4→Screen4, §5→Screen7, §6→Screen3, §7→Screen2, §8→Screen3, §9→Screen1, §10→Screen3, §11→Screen4, §12→Screen6, §13→Screen5 — all ids '1'..'13' present). Several sections END on a `QuizQuestion` screen that writes the id on the correct answer (§4/§6/§7/§8/§10/§11 final screens), and §9 is a single content screen that writes '9' on Next. The controller writes the section id on each section's last screen, reproducing the identical ['1'..'13'] array. `storageKey` is `@communication_mistakes_completed_sections` (confirmed in `src/constants/storageKeys.ts`).
- communicationMistakes (heading sizes): source headers are 32px on §1 screen1 (mapped to `size: 'xl'`), 24px and 26px elsewhere (mapped to `size: 'lg'`). The engine's `xl` = 28px and `lg` = 22px, so the 32px title renders smaller and the 24/26px headers render slightly smaller than the source; chosen the closest available sizes (matching the reference lessons' convention).
- communicationMistakes (dialogue / chat-bubble screens → paragraphs): several screens are speaker-labelled dialogues — a left/right chat-bubble layout (`dadBubble`/`daughterBubble`, §2 screen3, §3 screen2) or a stacked `dialogueCard` with bold `speaker:` labels above italic lines (§6 screen1, §7 screen1, §8 screen1, §12 screen1, §13 screen1). There is no dialogue/chat block in the schema; to preserve ALL text verbatim, each exchange is transcribed as a `paragraph` with the speaker name as an inline `emphasis` span followed by the plain dialogue text (mirroring helpingProcessEmotions §2 screen1). The chat-bubble left/right alignment, coloured bubbles, and per-speaker card styling are not reproduced. The §2 screen3 Dad bubble's mid-bubble emphasis line ("Maybe that's why…") is folded into the same paragraph's plain span (the source emphasised it inside one bubble); hard `\n` line breaks within a bubble are preserved.
- communicationMistakes §1 screen2 (dialogue box with "(Sighs)"): the source is a single blue-tinted (`#E3F2FF`) left-accented `dialogueBox` with an italic muted "(Sighs)" line above three dialogue lines. Transcribed as one `summary` callout with `(Sighs)` as the `label` and the three lines as callout lines, plus the helper as an italic `footer`. The label renders uppercase in the callout renderer (source "(Sighs)" was sentence-case italic), and the exact bubble styling/left border is approximated by `bg`.
- communicationMistakes (coloured / titled boxes → callouts): most content sits in coloured, sometimes titled rounded boxes — `Colors.primaryBg` (renderer default `summary` tint), amber `#FFF9E6`, orange `#FFF4E6`, off-white `#F9FAFB`, green `#E8F5E9`, blue `#E3F2FF`, and white `Colors.surface`. There is no titled-list block in the schema, so each is transcribed as a `callout` (`summary` for titled/bulleted/example/scenario boxes with the box's title as `label`; `insight` for the green key-insight/celebration boxes; `quote` for the Mark Twain box). One-off box backgrounds are carried via `bg` where distinctive (amber/orange/green/blue/off-white); primaryBg boxes use the renderer default. The source's exact left-vs-centre alignment, left-accent borders, and italic quote styling are approximated by the renderer defaults. All text preserved verbatim, including hard `\n` line breaks inside a box (kept within a single callout line).
- communicationMistakes (bulleted lists → cardLists): several screens use `•` bullet lists (§2 screen6/screen9, §5 screen3/screen7, §6 screen2, §8 screen2, §12 screen6, §13 screen3, §13 screen5). Transcribed as `plain` `cardList`s with `•` as an emoji icon per item. Where the source bullet colour is the amber/orange `#D97706` (the "what the upset person hears" boxes: §6 screen2, §8 screen2, §12 screen6, §13 screen3), that colour is carried via each item's `iconColor`; the default-primary bullets carry no override. The exact left-aligned inline bullet layout differs slightly from the renderer's icon-slot layout, and these bulleted items originally sat INSIDE coloured boxes (amber `#FFF9E6`) — the enclosing box background is not reproduced when the bullets are modelled as a `cardList`.
- communicationMistakes §2 screen9 (Key Takeaway): the source is one primaryBg box containing a bold title, two `•` bullets, a divider, then an emphasised "Notice → Empathize → Validate" line. Modelled as a titled `summary` callout (title as `label`, the two bullets as lines) followed by a second `summary` callout for the "Your job first is to:" line; the intra-box divider is not reproduced.
- communicationMistakes §3 screen4 / §5 (multi-region insight cards): screens like §3 screen4 combine an intro line, a subheading, and a centred quote list ("You're wrong" / "Your feelings aren't valid" / "I'm not on your side") inside one `#F9FAFB` box. Transcribed as a single `summary` callout with all lines in order; the source's decorative paired quote-mark glyphs (`"` before/after each phrase, rendered as separate coloured `Text` marks) are folded into the line text as surrounding `"` characters. The subheading/intro visual hierarchy within the box is flattened to sequential callout lines.
- communicationMistakes §1/§3/§12/§13 (section-label eyebrows): only some section-first screens carry an on-screen section label — §3 screen1 ("Section 3 of 13", muted grey uppercase), §12 screen1 ("Section 12 of 13", primary-coloured), §13 screen1 ("Section 13 of 13", primary-coloured). §1/§2 have NO section label, and the §4–§11 first screens have none either. Transcribed via the screen-level `label` field ('Section 3 of 13' / 'Section 12 of 13' / 'Section 13 of 13') on those three screens only, faithfully reproducing the source's inconsistent labelling. (The label renderer uppercases; the §3 source was already uppercase-styled, the §12/§13 sources were sentence-case primary text — colour/case differ but text is preserved.)
- communicationMistakes (hero emoji vs Ionicon): every hero graphic in this lesson is a literal emoji rendered as bare centred `Text` (⏸️, 💡, ✅, ⚠️, 🤔, ⚡, 💬, ✨, 🎯) at source sizes 40–64px on NO circle. Modelled with the `heroEmoji` block (emoji form), which renders the emoji at 50px inside a 100×100 tinted circle — the decorative circle and exact size differ, but the emoji is preserved. No Ionicon hero circles appear in this lesson (0 `icon`-form heroEmoji; all 11 are emoji-form).
- communicationMistakes §13 screen5 (celebration box): the final screen's green `#E8F5E9` `celebrationBox` stacks a large 🎉 emoji above the completion sentence. Modelled as an `insight` callout (green `bg`) with the 🎉 as its own line and the sentence as a second line; the standalone large-emoji styling is approximated as a text line.
- communicationMistakes (Next-gating / interactivity): this lesson has NO free-text `TextInput` or `EmotionPicker` screens — the "reflection" screens (§5 screen7, §13 screen4) are passive prompt cards with no input field, so they are plain content (no `textInput`/`emotionPicker` blocks). Quiz screens use the `QuizQuestion` component and are transcribed as `quiz` blocks (the controller advances on the correct answer). Block-type counts: 21 `quiz`, 0 `interactiveQuiz`, 0 `textInput`, 0 `emotionPicker`.

# Content errata — Flow Lessons 1-4

Issues found during the faithful transcription of the four hand-built "flow"
lessons (the numbered onboarding-style Lesson 1–4) in `src/screens/lessons/`
into `src/lessons/content/lesson{1,2,3,4}.ts`. Screen files per lesson:
Lesson1 = Screen1..11 + Quiz + QuizQ1..3 + Complete (16); Lesson2 = Screen1..12 +
Quiz + QuizQ1..3 + Complete (17); Lesson3 = Screen1..12 + Quiz + QuizQ1..6 +
Complete (20); Lesson4 = Screen1..11 + Quiz + QuizQ1..6 + Complete (19). Text was
copied VERBATIM (errors preserved); each observed problem is logged here rather
than silently fixed.

- flow lessons 1–4 (single-section, no storageKey): unlike the section-based lessons (5–13), the flow lessons are linear and do NOT persist section progress — each Complete screen navigates back to `MainTabs` (`rootNavigation.navigate('MainTabs')` in Lesson1, `navigation.getParent()?.navigate('MainTabs', { screen: 'Learn' })` in Lesson2/3/4) and writes NO AsyncStorage key. Accordingly each lesson is modelled as ONE `section` (id `'1'`) and `storageKey` is OMITTED entirely (the schema makes it optional; the controller skips the progress write when absent). Slugs are `lesson1`..`lesson4` (matching `LESSON_NAV['1'..'4']` in `src/navigation/lessonRoutes.ts`, which route to `Lesson{1..4}Screen1`). Titles use the lesson-hub wording from `LearnScreen.tsx` `learningModules` ids '1'..'4' — Lesson1 "What changed parenting Science?", Lesson2 "Happiness Chemicals", Lesson3 "The Long-Term Unhappiness Chemical", Lesson4 "The Long-Term Happiness Chemical" (the on-screen headers differ, e.g. Lesson1 screen1 header is "What Changed in How We Understand Parenting").
- flow lessons 1–4 (all screens): quotation marks and apostrophes are straight ASCII (`"..."`, `you'll`, `it's`, `isn't`, `doesn't`, `don't`, `That's`, `We'll`, `I'm`, `child's`, `Let's`, `you've`, `There's`, `children's`, `I'd`, `Dopamine Isn't`) rather than the curly typographic quotes used in some other lessons — inconsistent typography. The real em dash `—` (U+2014) is used throughout (both spaced "changed — and" and inside sentences). All transcribed verbatim as-is.
- flow lessons 1–4 (`LessonContainer` step denominator overshoots): every screen declares `totalSteps={15/16/20/19}` — exactly ONE more than the real screen count (16/17/20/19 respectively for Lesson1/2/… wait: Lesson1 has 16 screens but declares 15, Lesson2 has 17 but declares 16, Lesson4 has 19 but declares 19). Concretely: Lesson1 `totalSteps={15}` vs 16 screens (undershoots by one — `currentStep` runs 1→15 on the Complete? no, Complete is a `SafeAreaView` with no container/step); Lesson2 `totalSteps={16}` vs 16 in-container screens (the Complete screen is a bare `LinearGradient` with no `LessonContainer`, so the 16 stepped screens = Screen1..12 + Quiz + QuizQ1..3); Lesson3 `totalSteps={20}` covers Screen1..12 + Quiz + QuizQ1..6 = 19 stepped screens (Complete is uncontained) — overshoots by one; Lesson4 `totalSteps={19}` covers Screen1..11 + Quiz + QuizQ1..6 = 18 stepped screens (Complete uncontained) — overshoots by one. The Complete screen is NOT inside a `LessonContainer` in any flow lesson, so it shows no step chrome; all 16/17/20/19 screens are still transcribed as `content` screens (the controller renders its own progress). The step-denominator quirk is a source display artifact, not reproduced at the data level.
- flow lessons 1–4 (bold emphasis lines → `heading` size 'lg'): many screens have secondary bold statement lines (`bodyEmphasized`/`bodyHighlight`/`cardText`, fontSize 17–24, `Colors.primary`/`Colors.textPrimary`/`Colors.success`) rendered below the main headline. There is no bold-body-line block in the schema, so these are transcribed as `heading` `size: 'lg'` (engine `lg` = 22px; close but not byte-identical — some source lines were 17–19px, and the source green/primary colours are not reproduced by the heading renderer). Text preserved verbatim, including hard `\n` line breaks kept within a single heading string (e.g. Lesson3 screen10 "The good news:\nCortisol is highly responsive…").
- flow lessons 1–4 (bulleted lists → `plain` cardList with `•` emoji): every `•`-prefixed bullet list is transcribed as a `plain` `cardList` with `•` as an `emoji`-kind icon on each item. The source strips the leading `•` glyph into the text; here the `•` is the item's leading icon. Lesson1 screen8's positive bullets (`bulletPositive`, green `Colors.success`) and Lesson3 screen7's two-list layout are flattened to default-styled bullets — the green colour is NOT reproduced (cardList items carry no per-item text colour for these). The left-aligned inline bullet layout differs slightly from the renderer's icon-slot layout.
- flow lessons 1–4 (coloured/titled boxes → callouts): highlight/insight/comparison/analogy/memory boxes are transcribed as `callout` blocks. `Colors.primaryBg` boxes (Lesson1 screen5/7, Lesson3 screen5, Lesson4 screen9) → `summary`; italic-metaphor `primaryBg` boxes (Lesson2 screen9, Lesson3 screen8) → `insight`; `successBg` green boxes (Lesson3 screen11, Lesson4 screen5/10) → `summary`/`insight` with `bg: '#E8F5E9'`; the two-line "Old approach / Modern approach" comparison cards (Lesson1 screen9) → two `summary` callouts with the approach name as `label`; analogy `backgroundColor: Colors.backgroundGray` boxes (Lesson4 screen10) → plain `summary`. The source's exact background colours, left-accent borders, italic styling, and green/primary text colours are approximated by the renderer defaults; the multi-region box grouping (e.g. Lesson4 screen5's memory box wrapping intro + bullets + outro in one box) is flattened to sequential callouts + a cardList. All text preserved verbatim, including hard `\n` breaks kept inside a single callout line.
- flow lessons 1–4 (Screen4 double-quote lines, Lesson1): Lesson1 screen4's two italic primary quote lines ("How do we control children?" / "How do children's brains actually develop?") sit as bare centred italic `quote` text (not in a box). Transcribed as `quote`-variant callouts (the renderer's quote box adds a pink tinted card + shadow the source lacks); the interleaving "Instead of asking:" / "Researchers began asking:" subheads are plain `paragraph`s. Lesson2 screen8's single "If I had what they have, I'd be happier." quote is likewise a `quote` callout.
- flow lessons 1–4 (quiz intro screens): each Lesson{N}Quiz is a quiz-INTRO content screen (a 🎯 emoji + "Quest Time"/"Quest Time!" heading + subtitle + a "Start →"/"Start Quiz →" CTA). Transcribed as a normal `content` screen. Lesson1's 🎯 sits in a `primaryBg` tinted CIRCLE (`iconContainer`) → `heroEmoji` emoji form (the renderer's tint differs); Lesson2/3/4's 🎯 is a bare 64px centred emoji with no circle → also `heroEmoji` emoji form (the renderer adds a tinted circle the source lacks). CTAs preserved verbatim ("Start →", "Start Quiz →"). heroEmoji counts across the four lessons: 6 emoji-form (Lesson1 quiz 🎯 + Lesson1 Complete ✓; Lesson2 quiz 🎯; Lesson3 quiz 🎯 + Complete 🌿; Lesson4 quiz 🎯 + Complete 🌱 = actually 7 emoji) and 1 icon-form (Lesson2 Complete `checkmark` Ionicon). See "hero graphics" bullet below for the exact split.
- flow lessons 1–4 (hero graphics on Complete screens): Lesson1 Complete shows a large `✓` character in a green `Colors.success` circle → `heroEmoji` EMOJI form (emoji `'✓'`); the source is a literal check character, so emoji form (not the Ionicon `checkmark`) matches it. Lesson2 Complete shows an `Ionicons name="checkmark"` in a pink gradient circle → `heroEmoji` ICON form (`icon: 'checkmark'`) — the gradient fill and white glyph colour are not reproduced (renderer uses a flat tinted circle + primary-coloured glyph). Lesson3 Complete shows a 🌿 emoji in a pink gradient circle and Lesson4 Complete a 🌱 emoji in a green gradient circle → both `heroEmoji` EMOJI form (the gradient fill is not reproduced). Net heroEmoji counts: 7 emoji-form (Lesson1 quiz 🎯, Lesson1 Complete ✓, Lesson2 quiz 🎯, Lesson3 quiz 🎯, Lesson3 Complete 🌿, Lesson4 quiz 🎯, Lesson4 Complete 🌱) + 1 icon-form (Lesson2 Complete `checkmark`).
- flow lessons 1–4 (Complete screens → content, not sectionComplete): the Complete screens are rich content (animated checkmark/emoji + congrats headline + body + progress badge/card + a "NEXT:" preview + a CTA that returns to MainTabs). None matches the sparse canonical `sectionComplete` card, and (being the single section's last screen with no storageKey) no progress is written. Transcribed as `content` screens. The progress badge/card ("Lesson completed" for Lesson1; the "N/8 lessons completed" progress card for Lesson2/3/4) is transcribed as a `summary` callout (label `YOUR PROGRESS`, lines `N/8` + `lessons completed`); the source's badge circle / big-number styling is flattened to text. The "Up next:" / "NEXT:" preview lines are transcribed as an `eyebrow` + `paragraph` (Lesson1) or a `preview` callout with a `NEXT:` label (Lesson3/4). Complete CTAs preserved verbatim: Lesson1 "Continue to Learn →", Lesson2/3 "Start Next Lesson →", Lesson4 "Continue →".
- flow Lesson3 QuizQ2 / Lesson4 QuizQ2, QuizQ3, QuizQ6 (RESOLVED — engine extended): these four questions use the `QuizQuestionMultiSelect` component (MULTIPLE correct answers, select-all-that-apply, with a Check-Answer gate and retry-on-wrong). Rather than flatten them to single-answer `quiz` blocks (which would change the interaction), a `multiSelectQuiz` block was added to the SPEC-09 engine (Phase 3) — it reuses the existing `QuizQuestionMultiSelect` component verbatim, rendered by the `LessonController` parallel to the single-answer `quiz` branch. All question/option/feedback text and every `isCorrect` flag preserved verbatim. (All other quiz screens use single-answer `QuizQuestion` → `quiz`.) Quiz-block totals: Lesson1 = 3 quiz; Lesson2 = 3 quiz; Lesson3 = 5 quiz + 1 multiSelectQuiz; Lesson4 = 3 quiz + 3 multiSelectQuiz.
- flow Lesson2 Complete "2/8" wording: the source progress card renders `2` and `/8` as two separate `Text` nodes (`progressNumber` + `progressTotal`). Transcribed as a single callout line `'2/8'` (Lesson3 `'3/8'`, Lesson4 `'4/8'`); the two-tone big-number vs muted-total styling is not reproduced.
