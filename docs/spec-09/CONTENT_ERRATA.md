# Content errata — Sprinklers transcription

Issues found during the faithful transcription of the hand-built Sprinklers
screens into `src/lessons/content/sprinklers.ts`. Text was copied VERBATIM
(errors preserved); each observed problem is logged here rather than silently
fixed.

- sprinklers §5 screen1–6: `totalSteps={7}` is declared in every Section 5 screen, but the section only has 6 screens (SprinklersSec5Screen1..6, with `currentStep` running 1→6) — the progress denominator overshoots the real screen count by one.
- recordingDeepBondMoments §1 screen1–6: all quotation marks and apostrophes are straight ASCII (`"..."`, `weren't`, `don't`, `it's`, `you'll`, `can't`) rather than the curly typographic quotes (`"…"`, `'`) used across the other converted lessons — inconsistent typography. Transcribed verbatim as-is.

# Content errata — Emotional Sandbags transcription

Issues found during the faithful transcription of the hand-built Emotional
Sandbags screens into `src/lessons/content/emotionalSandbags.ts`. Text was
copied VERBATIM (errors preserved); each observed problem is logged here rather
than silently fixed.

- emotionalSandbags §3–§6 (all screens): quotation marks and apostrophes are straight ASCII (`"..."`, `isn't`, `it's`, `you're`, `wasn't`, `can't`, `Let's`, `you've`, `didn't`) rather than the curly typographic quotes (`"…"`, `'`) used in §1–§2 of the same lesson and in the other converted lessons — inconsistent typography within the lesson. Transcribed verbatim as-is.
- emotionalSandbags §5 screen6: the emotion-category labels embed literal emoji glyphs in the header text (`❌ DON'T USE (Too Broad)`, `🔵 UPSET EMOTIONS`, `🔴 ANGRY EMOTIONS`, `🟢 STRESSED / LOW-ENERGY`, `🟣 CONFUSION / SOCIAL PAIN`, `🟡 HAPPY EMOTIONS (for contrast later)`) — kept verbatim in the callout labels.
- emotionalSandbags §2 screen3: the original screen defines unused label-position styles (`label1/label2/label3`) and ends with a stray comment `// Re-adjusting labels in the code snippet for clarity`; the three floating labels (`STRESS`, `ANGER`, `OVERWHELM`) are absolutely-positioned decorations layered over the 🎒 emoji circle and carry no body text — the hero emoji is transcribed but the decorative overlay labels are not reproducible as data blocks.
- emotionalSandbags §1 screen3 / §2 screen4 / §5 screen8 & others: several screens open with a decorative Ionicon inside a coloured circle (e.g. `bulb-outline`, `people-outline`) rather than an emoji. The schema's `heroEmoji` block carries an emoji, not an Ionicon, so these purely-decorative Ionicon hero circles are omitted (no text is lost).
- emotionalSandbags §4 screen2–5: these are interactive accordions (one of four steps expanded, the rest collapsed as tappable previews). Transcribed as a static step-list `cardList` plus the expanded step's content blocks; the collapse/expand interaction is not reproducible in the data model.

# Content errata — Helping Someone Process Emotions transcription

Issues found during the faithful transcription of the hand-built Helping Someone
Process Emotions screens into `src/lessons/content/helpingProcessEmotions.ts`.
Text was copied VERBATIM (errors preserved); each observed problem is logged here
rather than silently fixed.

- helpingProcessEmotions §1–§2 (all screens): quotation marks and apostrophes are straight ASCII (`aren't`, `They're`, `you'll`, `Let's`, `That's`, `She's`, `don't`, `it's`, `Everyone's`, `I'm`, `you're`, `there's`, `didn't`, `wasn't`, `you can't`, `"fix it"`, `"perfect"`) rather than the curly typographic quotes (`"…"`, `'`) used across the other converted lessons — inconsistent typography. Transcribed verbatim as-is.
- dissociation §1–§4 (all screens): quotation marks and apostrophes are straight ASCII (`"..."`, `don't`, `can't`, `I've`, `isn't`, `there's`, `Let's`, `you're`, `child's`, `doesn't`, `you'll`, `I'm`, `I'll`, `That's`, `it's`, `dissociation's`) rather than the curly typographic quotes (`"…"`, `'`) used across the other converted lessons — inconsistent typography. Transcribed verbatim as-is.
- dissociation §1 screen4: the original screen is a multi-part comparison layout — a scenario card ("Imagine this:" + setup + a quote box), a divider, two coloured option cards (Option A — No dissociation, green `#E8F5E9`; Option B — Dissociation, orange `#FFF4E6`), then a Key-insight box. There is no comparison/two-column or nested quote-in-card block in the schema, so each region is transcribed as a separate `callout` (scenario summary, the quote as its own summary callout with the quote repeated as label+line, the two option cards as coloured summary callouts with the option name as `label`, and the key insight). The nested visual hierarchy and column styling are not reproduced; all text is preserved verbatim.
- dissociation §4 screen1: the three "Today we learned" bullets embed a hard line break (`\n`) inside the first bullet's text; kept verbatim inside the single `cardList` item title (the schema has no per-item multi-line/subtitle rendering for bullets).
- helpingProcessEmotions §2 screen1: the original screen is a Dad/Daughter dialogue card (`dialogueCard` with bold `speaker:` labels above italic `dialogue` lines) — there is no dialogue block in the schema, and the `cardList` block renders only an item title (subtitle is dropped). To preserve ALL dialogue text verbatim, each exchange is transcribed as a `paragraph` with the speaker name as an inline `emphasis` span followed by the plain dialogue text; the closing `helperBox` ("Notice what Dad isn't doing.") is transcribed as the italic `footer`. The two-column card styling is not reproduced.

# Content errata — Naming our Emotions transcription

Issues found during the faithful transcription of the 24 hand-built Naming
Emotions screens (Sub1..4, Screen1..6) in `src/screens/naming-emotions/` into
`src/lessons/content/namingEmotions.ts`. Text was copied VERBATIM (errors
preserved); each observed problem is logged here rather than silently fixed.

- namingEmotions §1–§4 (all screens): quotation marks and apostrophes are straight ASCII (`"..."`, `Let's`, `doesn't`, `We're`, `It's`, `don't`, `you're`, `there's`, `That's`, `you'll`, `You've`) rather than the curly typographic quotes (`"…"`, `'`) used in some other lessons. The screen-3 headings use a real Unicode horizontal ellipsis glyph `…` (U+2026) in "At the surface, this felt like…", and the callouts use a real em dash `—` (U+2014) and en dash `–` (U+2013, "2–3 emotions"). All transcribed verbatim as-is.
- namingEmotions §1–§4 screen2 / screen4 / screen5 (RESOLVED — engine extended): these are genuinely interactive screens — a free-text multiline `TextInput` ("What happened?", placeholder "Describe the moment briefly. Where were you? Who was involved?"), and emotion-picker screens (a picker button opening the shared `EmotionPicker` modal, with a conditional "Why did you feel {emotion}?" follow-up `TextInput`). Rather than drop the interactivity, two block types were added to the SPEC-09 engine (Phase 3): `textInput` and `emotionPicker` (see `schema.ts` / `BlockRenderer.tsx`). Screen 2 → `textInput`; screens 4 & 5 → `emotionPicker` (reusing the existing global `EmotionPicker` component verbatim). The user's input is EPHEMERAL — held in local state and discarded on completion — exactly as in the hand-built screens (which never persisted it). The `LessonController` gates its Next button (visible-but-disabled) until each required input is satisfied, mirroring the source's `disabled={value.trim().length === 0}` / `disabled={!canContinue}`.
- namingEmotions §1–§4 screen3 (RESOLVED — chip colours supported): the emotion "chip" (`HAPPY`/`SAD`/`MAD`/`BAD`) is a single centred pill with one-off per-sublesson colours — HAPPY: bg `#FFFBEB`, border `#F59E0B`, text `#D97706`; SAD: bg `#EFF6FF`, border `#3B82F6`, text `#2563EB`; MAD: bg `#FEF2F2`, border `#EF4444`, text `#DC2626`; BAD: bg `#F3F4F6`, border `#6B7280`, text `#4B5563`. The `cardList` chip item schema was extended with optional `color` (bg), `borderColor`, and `textColor` fields, so these distinct topic colours ARE now reproduced (single-item `chips` `cardList`).
- namingEmotions §1–§4 screen3 + screen6: the source "messageBox" and "infoBox" are `Colors.primaryBg`-tinted, left-aligned rounded boxes holding multiple body lines. They are transcribed as `callout` `highlight` blocks (each source line a separate callout line). The default `highlight` renderer centres text and uses `Colors.surface`, so the tint colour and left alignment are not byte-identical; text is preserved verbatim.
- namingEmotions §1–§4 screen6: the original completion screens are content screens (headline + tinted info box), NOT the sparse canonical `sectionComplete` card. Per the SPEC-09 controller model (all-`content`, like dissociation.ts), they are transcribed as `content` screens; the controller writes the section id ('1'..'4') to the storage key on each section's last screen, reproducing markSubLessonComplete's `['1','2','3','4']` array.
- namingEmotions §1–§4 screen2/4/5 (behavioural, minor): the hand-built input screens wrap their body in a `KeyboardAvoidingView` (content pinned top via `justifyContent: 'space-between'`). The generic `LessonController` renders content centred inside the shared `LessonContainer`, which is itself a `ScrollView` — so the keyboard cannot clip the field (it scrolls into view), but the exact push-up animation and top-pinned layout differ slightly. Not reproduced at the block level to avoid adding `KeyboardAvoidingView` to the controller for all ~300 screens; the ScrollView is a functional equivalent.
- namingEmotions §4 screen5: the picker button label here is "Select an emotion" (not the "(optional)" variant), and the code comment says "Require 2 emotions minimum (not optional)" while the disable logic (`selectedEmotion && why.trim().length > 0`) still allows advancing after a single second emotion — a minor logic/label inconsistency in the source, noted but not altered. Transcribed with the source's actual heading ("What was another emotion present?") and helper ("Try to identify at least one more.").

# Content errata — Serve and Return

Issues found during the faithful transcription of the 22 hand-built Serve &
Return screens (Sec1..6) in `src/screens/serveReturn/` into
`src/lessons/content/serveReturn.ts`. Text was copied VERBATIM (errors
preserved); each observed problem is logged here rather than silently fixed.

- serveReturn §1–§6 (all screens): quotation marks and apostrophes are straight ASCII (`"..."`, `you'll`, `it's`, `isn't`, `Doesn't`, `don't`, `I'm`, `can't`, `Let's`, `week's`, `child's`, `you're`, `someone's`, `We'll`, `There's`, `You're`) rather than the curly typographic quotes (`"…"`, `'`) used in some other converted lessons — inconsistent typography. Transcribed verbatim as-is. Note the em dash `—` (U+2014) in §1 screen1 ("70% of serves" line uses a real `—`) and the no-space em dash joins (`childhood—it's`, `communication.`, `connection—one`) are all straight-transcribed. §5 screen3 "Missing some serves is normal — and human." uses a spaced em dash.
- serveReturn §1 screen4 / §2 screen4 / §3 screen4: three `QuizQuestion` screens with a single question each (`totalQuestions={1}`). §1 screen4 writes `markSectionComplete('1')` and §2 screen4 writes `'2'` on correct answer (last screen of their sections); §3 screen4 instead NAVIGATES to screen5 on correct (screen5 is §3's real last screen and writes `'3'`). Transcribed as `quiz` blocks; the controller writes the section id on each section's last screen (screens 4/4/5 respectively), reproducing the `['1','2','3','4','5','6']` array.
- serveReturn §3 screen5 (OPTIONAL text input): the reflection field's `TextInput` (placeholder "Your reflections (optional)") is NOT gated — the source's "Finish" button (`onPress={handleFinish}`) has no `disabled` logic, so the field is genuinely optional. Modelled with a `textInput` block, `required: false`, `minHeight: 120` (source uses `minHeight: 120`). The source has NO bold headline above the input (the screen's `header` "Think About It" is the heading and a body paragraph precedes the input), so the `textInput` block's required `headline` field is set to the empty string `''`; the block therefore renders no headline text, matching the source. The user's input is EPHEMERAL (local `useState`, discarded on completion), matching the hand-built screen.
- serveReturn §1 screen2: the "back-and-forth" steps are a 2-item numbered card (`stepsCard`: circular primary-coloured number badges + text). Transcribed as a `surface` `cardList` with `number: 1` / `number: 2`. The renderer's number circle is a neutral surface-coloured badge rather than the source's filled `Colors.primary` circle with white text — the numbering is preserved but the badge colour differs.
- serveReturn §1 screen3: the tennis-match metaphor rows are emoji-led cards (🎾, 🏓, 🔄) in a single white `metaphorCard`. Transcribed as a `surface` `cardList` with emoji icons. The source groups all three rows inside one shared card; the `cardList` renders three separate surface cards — visually similar but not one enclosing box.
- serveReturn §2/§3/§4/§5/§6 (titled bullet/quote boxes): most content is coloured, titled rounded boxes holding bulleted or quoted lines — green `#E8F5E9`, amber `#FFF9E6`, `Colors.primaryBg`, blue `#E3F2FF`, and white surface. There is no titled-list block in the schema, so each is transcribed as a `callout` (`summary` for titled bullet/example boxes with the card title as `label`; `insight`/`quote`/`preview`/plain `summary` for the accent boxes). The source's exact background colour and LEFT text alignment are approximated: `summary` callouts render left-accented but the bullet `•` / check `✓` glyphs and coloured bullet colours are NOT reproduced (the lines render as plain centred/left callout lines without bullet markers). One-off box backgrounds are carried via `bg` where a green/amber/blue box is distinctive; boxes on `Colors.primaryBg` use the renderer default. All line text is preserved verbatim.
- serveReturn §4 screen1–3 / §5 screen1–3 / §6 screen1: the hero graphic is a large emoji (👋, 👁️, 📱, 🤗, 🧠, 👨‍👩‍👧, 👀) centred in a tinted rounded SQUARE box (`imageContainer`, `borderRadius` lg, `padding: 40`, emoji `fontSize: 80`). Modelled with the `heroEmoji` block, which renders the emoji in a tinted CIRCLE (100×100, `fontSize: 50`) — the shape (circle vs rounded square) and emoji size differ, but the emoji and its tint colour (`bg`: `#F5F9FF` for primaryBg boxes, `#E8F5E9` green, `#FFF9E6` amber, `#E3F2FF` blue) are preserved. (`Colors.primaryBg` is approximated as `#F5F9FF` to match the callout `summary` default tint; its exact theme value was not resolved.)
- serveReturn §4 screen1 / §5 screen1 / §6 screen1: only the FIRST screen of §4, §5, §6 carries a "Section N of 6" eyebrow label (`sectionLabel`); the other screens in those sections, and ALL of §1–§3, have no such label. Transcribed via the screen-level `label` field ('Section 4 of 6' / 'Section 5 of 6' / 'Section 6 of 6') on those three screens only, faithfully reproducing the source's inconsistent labelling. §1–§3 use no `label` at all (matching source, which shows only the `LessonContainer` step progress).
- serveReturn §5 screen3: the stats box contains an inline-emphasised span — "returning about **70% of serves** is enough…" (`statsHighlight`: bold, `Colors.primary`). Transcribed as a `paragraph`-style rich-text array inside a `summary` callout line, with "70% of serves" carrying `emphasis: 'emphasis'`. The callout renderer's emphasis style (primary + bold) matches the source's highlight styling.
- serveReturn §6 screen3: the "This week's practice" checklist uses literal `✓` check glyphs (`checkmark` text, green `#2E7D32`) as list markers. Transcribed as a `plain` `cardList` with `✓` as an emoji icon on each item; the check glyph is preserved but its green colour is not (emoji icons render as-is). The closing box (`transitionBox`, "We'll build on this in the next lesson.") is a muted surface preview — transcribed as a `preview` callout.
- serveReturn §1–§6 (all sections): none of the section-final screens is the sparse canonical `sectionComplete` card. §1/§2/§3 end on a quiz or reflection screen; §4/§5/§6 end on a normal content screen. Per the SPEC-09 controller model (all-`content`, like dissociation.ts / namingEmotions.ts), every screen is `content`; the controller writes the section id on each section's last screen, reproducing `markSectionComplete`'s `['1'..'6']` array.
- serveReturn heading sizes: source headers are 28px on §1 screen1 (mapped to `size: 'xl'`) and 26px everywhere else (mapped to `size: 'lg'`). The engine's `xl` = 28px and `lg` = 22px, so the 26px headers render slightly smaller than the source; chosen `lg` as the closest available size (matching the reference lessons' convention).
