# spec-09/ — SPEC-09 working artifacts (frozen)

Snapshots from the SPEC-09 data-driven lesson-engine migration: the empirical block survey (`PHASE1_BLOCK_SURVEY.md`) and the verbatim-fidelity errata log (`CONTENT_ERRATA.md`). Frozen migration history — not current process. See `../README.md` for the live docs.
