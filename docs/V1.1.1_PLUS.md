# v1.1.1+ — post-shipping polish and feature completeness

Beyond onboarding polish (see V1.1.1_ONBOARDING_POLISH.md), the prod
bug hunt on 2026-07-04 surfaced several items that are real work but
not shipping blockers for v1.1.0.

---

## Lesson 10 (Helping Someone Process Emotions) is a placeholder

**Problem:** The live `HelpingSomeoneProcessEmotionsLessonScreen.tsx`
renders its sub-lessons as static `<View>` cards with "Coming Soon"
badges. No `onPress` handler, no navigation into lesson content. Users
who tap lesson 10 from LearnScreen get a marketing page and can only
go back.

**But:** the full implementation existed in
`HelpingProcessEmotionsLessonScreen.tsx` (deleted 2026-07-04 as dead
code per Fable review #4). That file had 4 real sub-lessons wired to
LessonFlow navigation. Someone registered the placeholder as live and
orphaned the working file.

**Options:**
- (a) Accept placeholder as intended — this is a "Coming Soon"
  lesson. Update the LearnScreen card to say "Coming Soon" too so
  users don't feel bait-and-switched.
- (b) Copy the deleted file's sub-lesson launcher logic (fetch it
  from git history, commit fa27f90's parent or earlier) into the live
  file so lesson 10 has real content.

**Not v1.1.0:** current behavior (placeholder cards) matches prod
v1.0.0. This refactor didn't regress anything, it just didn't fix it.

---

## Lesson progress not synced to Supabase

**Problem:** `src/services/lessonProgressService.ts` has 7 functions
querying the `lesson_progress` table in Supabase — but **nothing in the
app calls any of them**. The service is 100% dead code, and the
matching `lesson_progress` table in Supabase is dead schema (empty for
every user).

Lesson progress today is stored per-lesson in AsyncStorage via
utility files like `src/utils/sprinklersProgress.ts`,
`src/utils/communicationMistakesProgress.ts`, etc. Each lesson has its
own storage key.

**Consequences:**
- Force-quit + reopen → progress survives (AsyncStorage persists) ✅
- Uninstall + reinstall → progress lost ❌
- Signed in on second device → no progress there ❌
- User buys new iPhone → starts over ❌

**Fix scope (~2-3 hours):**
- Modify each of the ~8 progress-tracking utility files to also write
  to Supabase (upsert into `lesson_progress` keyed on user_id +
  lesson_id).
- Add a hydration step on app launch that pulls the user's Supabase
  progress into AsyncStorage if it's more complete.
- Handle the merge-conflict case (device has progress, so does
  server) with last-write-wins or a smarter merge.

**Not a v1.1.0 concern.** Prod has always worked this way; users on the
same device don't notice. Cross-device is a real feature to build
later.

---

## Notifications feature not implemented

**Problem:** Onboarding asks users if they want notifications.
Preference saved to `user_profiles.notifications_enabled`. But no code
path anywhere in the app actually schedules or sends any notification.
`expo-notifications` isn't imported for scheduling anywhere.

Every user in prod has been asked about notifications, said yes or no,
and never received any.

**Options:**
- Build the feature (schedule daily/weekly reminders based on
  preference). Not trivial — requires push notification server-side
  setup, deep-link handling on tap, permission timing.
- Remove the question from onboarding entirely so we're not
  dishonestly asking about a feature that doesn't exist.

**Not a v1.1.0 concern** — the current state (silently ignoring the
preference) doesn't hurt users, it just doesn't help them.

---

## Alert.alert() error message rework

**Problem:** ~14 different failure modes in the app all use the same
`Alert.alert("[X] Failed", "[Y] could not be completed. Please try
again[ or contact support]")` template. None tell the user:
1. What actually went wrong
2. What to do about it (retry? update app? contact support with
   what info?)
3. How to escalate (support email visible or buried?)

**Fix ideas (v1.1.1):**
- Categorize errors: network / server / provider / user-cancelled /
  auth-expired.
- Inline errors near the button that triggered them, not modal
  alerts (looks less like a crash to the user).
- On repeat failures, offer a "Report this to support" button that
  copies the error object + a correlation ID for our Sentry logs.
- Standardize the copy: "Something went wrong on our end" for server
  errors vs "Check your connection" for network errors.

**Not a v1.1.0 concern** — pattern is systemic, needs design pass, not
a per-alert fix.

---

## Delete old / dead code

**Problem:** Multiple dead-code paths surfaced today:
- `lessonProgressService.ts` — all 7 functions unused
- `ChildrenGenderScreen.tsx` — registered as a route, nothing navigates
  to it
- `updateChildGender` action in onboarding store — never called
  (kept `updateChildGender` since it's exposed API, might be useful
  later — but the "Optional Expand" section in ChildrenCountScreen
  that would have called it is commented out)
- The dead `showPersonalization` toggle inside ChildrenCountScreen
  (commented out but the state variable + section still exist)

**Fix (~30 min):** grep for orphaned exports and delete. Reduces bug
surface. TypeScript will catch anything that actually needs the
removed code.

**Not a v1.1.0 concern** but do this pass in v1.1.1 to clean the
codebase.

---

## Section 4: Data integrity checks not fully covered

The bug hunt Section 4 was mostly deferred:
- 4.2 (progress persistence): now documented above.
- 4.3 (cross-device sync): needs a second device, or a proper e2e test
  harness.
- 4.4 (edit onboarding answers): does Settings even have edit-profile
  options? Not audited. Small check to add to v1.1.1 test list.

---

## Family Sharing subscription

Not tested. Needs a second Apple ID that's a family member of the
purchasing Apple ID. Deferred to a real family testing setup or the
first customer complaint.
